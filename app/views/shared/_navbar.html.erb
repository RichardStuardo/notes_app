<nav class="navbar navbar-expand-lg navbar-color navbar-dark bg-dark  fixed-top">
    <a class="navbar-brand" href="<%root_path%>">
          <img class="logo2" src="<%= asset_path 'logo4.png' %>" alt="">
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <form class="form-inline my-2 my-lg-0">
          <% if user_signed_in? %>
              <a class="navbar-brand" href="<%= root_path %>">Hi! <%= current_user.name %></a>
          <% end %>
      </form>
      <ul class="navbar-nav ml-auto"> 
        <% if user_signed_in? %>
              <form class="form-inline my-2 my-lg-0 buscador">
                  <input id="buscador" class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                  <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
              </form>
              <li class="nav-item active">
                <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'btn btn-warning btn-md navbar-brand' %>
              </li>
            <% else %>
              <li class="nav-item active">
                <a class="nav-link btn btn-info btn-md"  href="<%= new_user_session_path %>">Log in</a>
              </li>
              <li class="nav-item active">
                <a class="nav-link btn btn-warning " href="<%= new_user_registration_path %>">Sign up</a>
              </li>
            <% end %>
              <li class="nav-item active">
                <a class="navbar-brand" href="<%= admin_user_session_path %>">
                    <img class="admin-logo " src="<%= asset_path 'logo-admin.ico' %>" alt="">
                </a>
              </li> 
      </ul>
    </div>
  </nav> 