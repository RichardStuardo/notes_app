
<%= form_with(model: note, :html => { :id => 'note_form' }) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>
      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.text_field :content, :html => { :id => 'email' }, class:'btn btn-lg' %>
    <%= form.submit 'Post'  , :class => 'sbtn btn-info btn-lg'%>
  </div>
<% end %>





